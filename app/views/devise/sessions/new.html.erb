<!DOCTYPE html>
<html>
  <head>
    <title>TarefaPlus | Login</title>
  </head>
  <body>
    <%= render 'layouts/navbar' %>

    <div class="container d-flex justify-content-center align-items-center min-vh-100">
      <div class="card p-4" style="width: 400px;">
        <h2 class="mb-4 text-center" style="color: #6a1b9a;">Login</h2>
        <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
          <div class="mb-3">
            <%= f.label :email, "Email", class: "form-label" %>
            <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control", placeholder: "Digite seu email" %>
          </div>
          <div class="mb-3">
            <%= f.label :password, "Senha", class: "form-label" %>
            <%= f.password_field :password, autocomplete: "current-password", class: "form-control", placeholder: "Digite sua senha" %>
          </div>
          <% if devise_mapping.rememberable? %>
            <div class="form-check mb-3">
              <%= f.check_box :remember_me, class: "form-check-input" %>
              <%= f.label :remember_me, "Lembrar-me", class: "form-check-label" %>
            </div>
          <% end %>
          <div class="d-grid">
            <%= f.submit "Entrar", class: "btn btn-primary" %>
          </div>
        <% end %>
        <hr>
          <div class="text-center">
            <p>
              <%= link_to "Cadastre-se aqui", new_registration_path(resource_name), style: "color: #6a1b9a; text-decoration: none;" %>
            </p>
            <p>
              <%= link_to "Esqueceu sua senha?", new_password_path(resource_name), style: "color: #6a1b9a; text-decoration: none;" %>
            </p>
          </div>
      </div>
    </div>
  </body>
</html>
